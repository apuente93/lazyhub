<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="span8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
<% provide(:title, 'the entertainment system for the web') %>
<div class="center hero-unit">
  <h1>Lazy Hub: the entertainment system for the web</h1>
  <%= render 'layouts/content' %>
</div>
<% end %>
