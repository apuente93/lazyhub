<div class="sign_in_up_box">
<div class="row">
<div class="span6 forms">
<h3 class="sign_up2">New Article</h3>
<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :image_tag %>
    <%= f.text_area :image_tag %>
  </div>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>
  <div class= "field">
    <%= f.label :article_type %>
    <% if URI(request.referer).path == '/game' %>
    <%= f.select :article_type, options_for_select(%w[Game]) %>
    <% end %>
    <% if URI(request.referer).path == '/laugh' %>
    <%= f.select :article_type, options_for_select(%w[Laugh]) %>
    <% end %>
    <% if URI(request.referer).path == '/news' %>
    <%= f.select :article_type, options_for_select(%w[News]) %>
    <% end %>
    <% if URI(request.referer).path == '/tech' %>
    <%= f.select :article_type, options_for_select(%w[Tech]) %>
    <% end %>
    <% if URI(request.referer).path == '/social' %>
    <%= f.select :article_type, options_for_select(%w[Social]) %>
    <% end %>
    <% if URI(request.referer).path == '/sports' %>
    <%= f.select :article_type, options_for_select(%w[Sports]) %>
    <% end %>
    <% if URI(request.referer).path == '/random' %>
    <%= f.select :article_type, options_for_select(%w[Random]) %>
    <% end %>
    <% if URI(request.referer).path == '/' %>
    <%= f.select :article_type, options_for_select(%w[Laugh Game News Tech Social Sports Random]) %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit "Create New Article", class: "btn btn-md btn-primary center-block" %>
  </div>
<% end %>
</div>
</div>
</div>
